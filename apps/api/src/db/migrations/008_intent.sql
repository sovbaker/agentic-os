-- Объявленное намерение агента.

/* Потолок доверия MVP — «делает с подтверждением», а подтверждать надо
   что-то конкретное, а не строку «подтвердить действие?». Здесь лежит то,
   что агент собирается сделать: заголовок, что станет и что уйдёт наружу.

   Отдельной миграцией, а не дописыванием в 007: применённая миграция
   не перезапускается, поэтому правка уже применённого файла молча не
   доедет до базы, где он уже отработал. */
ALTER TABLE job ADD COLUMN IF NOT EXISTS intent jsonb;
