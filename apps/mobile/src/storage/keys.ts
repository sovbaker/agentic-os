/**
 * Имя ключа живёт отдельным файлом, а не в `index.ts`.
 *
 * Метро на устройстве подставляет вместо `./index` файл `index.native.ts`,
 * поэтому импорт `./index` из него самого замкнулся бы на себя. Общая
 * константа обязана лежать там, куда платформенная подстановка не достаёт.
 */
export const TOKEN_KEY = 'agentic-os/device-token';
