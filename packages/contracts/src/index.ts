/**
 * @agentic-os/contracts — единственный источник правды для схем.
 *
 * Используется клиентом, сервером и генерацией. Расхождение контрактов между
 * клиентом и сервером — самая частая причина боли в SDUI-системах, поэтому
 * пакет общий, а не продублированный.
 */

export * from './ui-spec.js';
export * from './life-graph.js';
export * from './job.js';
